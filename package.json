{
  "name": "kaizerchiefs-redesign",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean": "rimraf dist",
    "start": "npm run clean && parcel serve ./src/pages/*.html",
    "build": "npm run clean && parcel build ./src/pages/*.html --no-source-maps",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Steven Selolo",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-image": "^2.0.0-rc.0",
    "@parcel/transformer-sass": "^2.0.0-rc.0",
    "parcel": "^2.0.0-rc.0",
    "posthtml-include": "^1.7.1"
  },
  "dependencies": {
    "@popperjs/core": "^2.10.1",
    "bootstrap": "^5.1.1",
    "gsap": "^3.7.1",
    "locomotive-scroll": "^4.1.2",
    "rimraf": "^3.0.2"
  },
  "browserslist": [
    "defaults"
  ]
}
